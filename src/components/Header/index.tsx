import { Logo } from "../../shared/assets/icons/logo";
import {Wrapper} from "../../styles";

const Navigation = () => {
    return (
        <Wrapper>
            <Logo/>
            <nav>Navigation</nav>
        </Wrapper>
    );
};

export default Navigation;
